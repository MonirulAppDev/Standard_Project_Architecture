import 'package:flutter_test/flutter_test.dart';
import 'package:spa_v2/core/services/crashlytics_service.dart';

void main() {
  test('CrashlyticsService placeholder test', () {
    final service = CrashlyticsService();
    expect(service, isNotNull);
  });
}
